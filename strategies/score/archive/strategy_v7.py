#!/usr/bin/env python3
"""
Score 策略 v7 — 基于 IC/IR 分析结果的权重优化方案

【分析结论摘要】
✅ 强正向因子（可加大权重）:
  - CCI信号(超卖): ICIR=+2.52, 100%正IC → 最重要!
  - Williams%R信号(超卖): ICIR=+0.84, 75%正IC → 第二重要
  - KDJ金叉: ICIR=+0.50, 75%正IC → 可保留

❌ 负向因子（需反向使用或移除）:
  - RSI绝对值: ICIR=-1.92, 0%正IC → RSI越高越跌? 需反思
  - 均线多头层数: ICIR=-1.90, 0%正IC → 多头排列反而跌?
  - 换手率: ICIR=-1.84, 12%正IC → 高换手是陷阱!
  - BOLL位置: ICIR=-1.75, 0%正IC → 贴近上轨反而跌
  - 量比: ICIR=-0.90, 25%正IC → 放量未必是好事

⚠️ 无效因子（可移除）:
  - MACD金叉: ICIR=+0.04, 50%正IC → 几乎没有预测力

【优化方案】

方案A — 激进版（完全基于IC数据重构）
=========================================
将因子分为三类，分别给正向权重:

第一梯队（保留且加大）:
  - cci_oversold (CCI < -100):     权重 0.35  ← 最大权重！
  - wr_oversold   (WR < -80):      权重 0.20

第二梯队（保留且修正方向）:
  - rsi_bucket (RSI 30-50区间):    权重 0.15  ← RSI低反而好
  - ma_layers (层数越少越好):       权重 0.10  ← 反过来用
  - turnover_bucket (换手率极低):  权重 0.10  ← 低换手好

第三梯队（保留但降权）:
  - kdj_cross (金叉):              权重 0.05  ← 微弱正向
  - volume_ratio (低量比):          权重 0.05  ← 反向用

移除第四梯队（表现太差）:
  - macd_condition: 移除（ICIR=0.04，几乎无效）
  - bollinger_condition: 移除（ICIR=-1.75）
  - macd_jc: 移除（与kdj_cross重叠）

方案B — 稳健版（保留原框架，仅调整权重）
=========================================
原框架整体保留，只做三件事:
1. 大幅提升CCI权重: 0.0597 → 0.30
2. 大幅降低RSI权重: 0.0597 → 0.02（甚至移除）
3. 增加WR超卖信号: 新增，给权重0.15

方案C — 实验版（专门针对负向因子的反转策略）
=========================================
这是一个"逆直觉"策略，专门利用市场的反转效应:
  - 买入条件: RSI超卖区(30以下) + 换手率极低(1%以下) + CCI超卖
  - 逻辑: 市场过度悲观后容易反弹

【推荐执行顺序】
1. 先用方案B跑历史回测（风险最低）
2. 方案A如果回测好，可以上模拟盘验证
3. 方案C作为探索性仓位单独测试

【需要补充的IC分析】
1. RSI分桶细化: RSI<30 / 30-50 / 50-60 / 60-70 / >70 各档位的IC
2. 量比的最佳阈值: 量比<0.5 / 0.5-1 / 1-2 / >2 各档位的IC  
3. 换手率的最佳阈值: 找到"高换手陷阱"的临界点
4. 基本面因子: PE/ROE/净利润增速的IC（目前尚未测试）
"""

import json
from datetime import datetime

OUTPUT = __file__
print(__doc__)
